import styled from "styled-components";

const Section = styled.section`
  & > div {
    background-color: #eee;
    margin: 0 auto;
    padding: 1.5rem;
    width: 450px;
    height: 700px;
  }
`;

export { Section };
